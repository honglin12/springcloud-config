spring:
  profiles:
    active:
    - dev   #当前使用环境
---
spring: 
  profiles: dev #当前使用环境
  application:
    name: config-server-dev
---
spring:
  profiles: test    #测试环境
  application:
    name: config-server-test